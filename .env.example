# Copy to .env and fill in values

HF_TOKEN=
FAL_KEY=

# Optional: Hugging Face Inference provider for wan-inference.py (default: fal-ai)
# HF_INFERENCE_PROVIDER=fal-ai

# Optional: SQLAlchemy database URL (default: data/wan_video.db under project root)
# DATABASE_URL=sqlite:///C:/path/to/wan-video/data/wan_video.db
